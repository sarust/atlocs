%section.main-content
  .container
    .row
      =render 'shared/adminmenu'
      .col-md-12
        = flash_messages
        %h1
          =@users.count
          Usuarios
        %table.table.table-striped
          %thead
            %tr
              %th Nombre
              %th Fono
              %th{:colspan => "3"}
          %tbody
            - @users.each do |user|
              %tr
                %td= user.email
                %td= user.phone
                %td= link_to 'Show', '/users/'+user.id.to_s
                %td= link_to 'Edit', '/users/edit'
                %td= link_to 'Destroy', '/users/'+user.id.to_s, method: :delete, data: { confirm: 'Are you sure?' }
        %br/
        = link_to 'New user', '/signup'